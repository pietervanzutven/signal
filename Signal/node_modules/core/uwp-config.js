(function () {
    'use strict';

    const localSettings = Windows.Storage.ApplicationData.current.localSettings;

    window.uwp_config = function UWPConfig() {
        this.get = name => localSettings.values[name];
        this.set = (name, value) => localSettings.values[name] = value;
    }
})();