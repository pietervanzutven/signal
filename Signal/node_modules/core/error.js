(function () {
    'use strict';

    window.Error.captureStackTrace = target => {
        target.stack = window.stacktrace.getSync().slice(1);

        Error.prepareStackTrace('', target.stack);
    }

})();