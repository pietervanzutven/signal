'use strict';

(function () {
    window.path = {
        join: function () {
            return Array.prototype.slice.call(arguments).join('/');
        },
        normalize: path => path,
    }
    window.__dirname = 'ms-appx://';
})();