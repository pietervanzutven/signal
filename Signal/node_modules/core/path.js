'use strict';

(function () {
    window.path = {
        join: function () {
            return Array.prototype.slice.call(arguments).join('/');
        },
        normalize: path => path,
        relative: (dir, file) => file.replace(dir),
    }
    window.__dirname = 'ms-appx://';
})();