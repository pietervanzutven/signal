(function () {
    'use strict';

    const fs = window.fs;

    window.firstline = file => {
        const text = fs.readFileSync(file);
        const lines = text.split('\n');
        return lines[0];
    }
})();