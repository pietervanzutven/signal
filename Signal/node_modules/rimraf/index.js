(function () {
    'use strict';

    window.rimraf = {
        sync: async dir => {
            const folder = await Windows.Storage.ApplicationData().localFolder.getFolderAsync(dir);
            folder.deleteAsync();
        }
    }

})()