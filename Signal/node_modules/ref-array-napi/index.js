(function() {
    'use strict';

    function Uint8Buffer(buffer) {
        return buffer;
    }

    function RefArray(type) {
        if (type === 'uint8') {
            return Uint8Buffer;
        }
    }

    window['ref-array-napi'] = RefArray;

})();