(function() {
    'use strict';

    const fs = window.fs;

    window.read_last_lines = {
        read: (file, n) => {
            const text = fs.readFileSync(file);
            const lines = text.split('\n');
            const m = -Math.min(n, lines.length);
            return lines.slice(m).join('\n');
        }
    }
})();