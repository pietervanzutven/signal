(function () {
  window.util = window.util || {};
  window.util.support = window.util.support || {};
  window.util.support.isBuffer = function isBuffer(arg) {
    return arg instanceof Buffer;
  }
})();